/*
 * PostPageAdd Messages
 *
 * This contains all the text for the PostPageAdd container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PostPageAdd';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PostPageAdd container!',
  },
});
